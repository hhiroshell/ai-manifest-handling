apiVersion: v1
kind: Secret
metadata:
  name: {{ include "bookstore.fullname" . }}-db-credentials
  labels:
    {{- include "bookstore.labels" . | nindent 4 }}
type: Opaque
data:
  username: {{ .Values.secret.dbCredentials.username }}
  password: {{ .Values.secret.dbCredentials.password }}
