apiVersion: apps/v1
kind: Deployment
metadata:
  name: bookstore-api
spec:
  replicas: 5
  template:
    spec:
      containers:
        - name: api
          image: bookstore/api:v1.2.0
          resources:
            limits:
              cpu: 1000m
              memory: 1Gi
